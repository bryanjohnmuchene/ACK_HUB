name: ack_hub
description: ACK Hub - the digital home for the Anglican Church of Kenya.
publish_to: none
version: 0.2.0+1
environment:
  sdk: '>=3.4.0 <4.0.0'
dependencies:
  flutter: { sdk: flutter }
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^9.2.2
  http: ^1.2.2
  provider: ^6.1.2
dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
